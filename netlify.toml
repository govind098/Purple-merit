[build]
  # Netlify site base directory is set to `frontend` in the UI.
  # When base is `frontend` the build should run from that directory.
  command = "npm run build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
